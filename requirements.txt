mkdocs==1.6.1
mkdocs-material==9.6.14
pymdown-extensions==10.15
# Pygments <2.20 is required: pymdownx 10.15 has a known incompatibility
# with pygments 2.20 (it passes filename=None to HtmlFormatter when a code
# block has no title, which crashes html.escape). Pin until upstream fixes it.
pygments>=2.18,<2.20
